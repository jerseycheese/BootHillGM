/**
 * AI Decision Constants
 * 
 * Constants used by the AI decision system
 */

// Default threshold for presenting a decision
export const DEFAULT_DECISION_THRESHOLD = 0.65;

// Minimum time between decisions in milliseconds (30 seconds)
export const MIN_DECISION_INTERVAL = 30 * 1000;

// Maximum number of decision options to present
export const DEFAULT_MAX_OPTIONS = 4;

// Maximum number of decisions to keep in history
export const MAX_DECISION_HISTORY = 10;

// Default API timeout in milliseconds
export const DEFAULT_API_TIMEOUT = 15000;

// Default maximum API retries
export const DEFAULT_MAX_RETRIES = 3;

// Default API rate limit
export const DEFAULT_RATE_LIMIT = 10;
