/**
 * This is a dummy test file to satisfy Jest's requirement that test directories contain test files.
 * It ensures that the __testUtils__ directory is not treated as a test suite itself.
 */

describe('Dummy test for utils directory', () => {
  it('passes to make Jest happy', () => {
    expect(true).toBe(true);
  });
});
